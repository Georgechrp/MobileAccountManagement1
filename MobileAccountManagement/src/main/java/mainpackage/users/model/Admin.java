package mainpackage.users.model;

public class Admin extends User{

    public Admin(String username, String name, String surname, String password, int role) {
        super(username, name, surname, password, role);
    }
}